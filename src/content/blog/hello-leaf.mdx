export const meta = {
  title: "Teste de encanamento",
  description: "Post descartável para validar a rota /blog/[slug].",
  date: "2026-08-11",
  tags: ["leaf"],
};

Parágrafo de teste com `código inline` e um link para
[/projetos/leaf](/projetos/leaf).

```rust
fn main() {
    println!("oi");
}
```

- item de lista
- outro item
